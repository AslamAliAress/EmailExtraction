import { AccAddress, Account, ModuleAccount, AuthParamsV1B1 } from '../../../core';
import { BaseAPI } from './BaseAPI';
import { APIParams } from '../APIRequester';
import { LCDClient } from '../LCDClient';
export declare class AuthAPI extends BaseAPI {
    lcd: LCDClient;
    constructor(lcd: LCDClient);
    /**
     * Looks up the account information using its Xpla account address. If the account has
     * vesting, it will be one of [LazyGradedVestingAccount, DelayedVestingAccount, PeriodicVestingAccount, ContinuousVestingAccount]
     *
     * @param address address of account to look up
     */
    accountInfo(address: AccAddress, params?: APIParams): Promise<Account>;
    moduleAccount(name: string, params?: APIParams): Promise<ModuleAccount>;
    moduleAccounts(params?: APIParams): Promise<ModuleAccount[]>;
    parameters(params?: APIParams): Promise<AuthParamsV1B1>;
}
