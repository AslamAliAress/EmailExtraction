import { EvmAddress, Numeric } from '../../../core';
import { EvmTx, EvmMessage } from './EvmTx';
/**
 * A basic message for sending [[Coins]] between Xpla accounts.
 */
export declare class EvmSendToken extends EvmMessage {
    contract: EvmAddress;
    from_address: EvmAddress;
    to_address: EvmAddress;
    amount: Numeric.Input;
    /**
     * @param contract erc20 token contract address
     * @param from_address sender's address
     * @param to_address recipient's address
     * @param amount value of the transaction
     */
    constructor(contract: EvmAddress, from_address: EvmAddress, to_address: EvmAddress, amount: Numeric.Input);
    tx(): EvmTx;
}
export declare class EvmMintToken extends EvmMessage {
    contract: EvmAddress;
    owner_address: EvmAddress;
    to_address: EvmAddress;
    amount: Numeric.Input;
    /**
     * @param contract erc20 token contract address
     * @param owner_address contract owner's address
     * @param to_address recipient's address
     * @param amount value of the transaction
     */
    constructor(contract: EvmAddress, owner_address: EvmAddress, to_address: EvmAddress, amount: Numeric.Input);
    tx(): EvmTx;
}
