import { EvmAddress, Numeric } from '../../../core';
import { EvmTx, EvmMessage } from './EvmTx';
/**
 * A basic message for sending [[Coins]] between Xpla accounts.
 */
export declare class EvmSendNft extends EvmMessage {
    contract: EvmAddress;
    from_address: EvmAddress;
    to_address: EvmAddress;
    id: Numeric.Input;
    /**
     * @param contract erc721 token contract address
     * @param from_address sender's address
     * @param to_address recipient's address
     * @param id NFT's ID
     */
    constructor(contract: EvmAddress, from_address: EvmAddress, to_address: EvmAddress, id: Numeric.Input);
    tx(): EvmTx;
}
export declare class EvmMintNft extends EvmMessage {
    contract: EvmAddress;
    owner_address: EvmAddress;
    to_address: EvmAddress;
    id: Numeric.Input;
    uri: string;
    /**
     * @param contract erc20 token contract address
     * @param owner_address contract owner's address
     * @param to_address recipient's address
     * @param id NFT's ID
     * @param uri NFT's data URI
     */
    constructor(contract: EvmAddress, owner_address: EvmAddress, to_address: EvmAddress, id: Numeric.Input, uri: string);
    tx(): EvmTx;
}
