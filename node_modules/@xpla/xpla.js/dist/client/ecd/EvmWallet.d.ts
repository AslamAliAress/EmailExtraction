import { Key } from '../../key';
import { CreateEvmTxOptions } from './api/TxAPI';
import { ECDClient } from './ECDClient';
import { EvmTx } from './msgs';
export declare class EvmWallet {
    ecd: ECDClient;
    key: Key;
    constructor(ecd: ECDClient, key: Key);
    accountNumberAndSequence(): Promise<{
        account_number: number;
        sequence: number;
    }>;
    accountNumber(): Promise<number>;
    nonce(): Promise<number>;
    createAndSignTx(options: CreateEvmTxOptions & {
        sequence?: number;
    }): Promise<EvmTx>;
}
