"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.EvmAddress = void 0;
var eip55_1 = require("eip55");
function checkPrefixAndLength(data, length, strict) {
    if (strict === void 0) { strict = true; }
    if (strict) {
        return (0, eip55_1.verify)(data);
    }
    if (data.startsWith('0x')) {
        var address = data.substring(2);
        try {
            var bytes = Buffer.from(address, 'hex');
            return bytes.length === length;
        }
        catch (_a) {
            return false;
        }
    }
    return false;
}
var EvmAddress;
(function (EvmAddress) {
    /**
     * Checks if a string is a valid evm account address.
     *
     * @param data string to check
     */
    function validate(data, strict) {
        if (strict === void 0) { strict = true; }
        return checkPrefixAndLength(data, 42, strict);
    }
    EvmAddress.validate = validate;
})(EvmAddress = exports.EvmAddress || (exports.EvmAddress = {}));
//# sourceMappingURL=eip55.js.map