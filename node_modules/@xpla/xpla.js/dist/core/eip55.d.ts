/** `0x-` prefixed account address */
export declare type EvmAddress = string;
export declare namespace EvmAddress {
    /**
     * Checks if a string is a valid evm account address.
     *
     * @param data string to check
     */
    function validate(data: string, strict?: boolean): boolean;
}
